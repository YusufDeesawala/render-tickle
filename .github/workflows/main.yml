name: Keep Render Server Alive

on:
  schedule:
    - cron: "*/5 * * * *" 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Ping
        run: curl https://meet-sync-backend-2.onrender.com/
